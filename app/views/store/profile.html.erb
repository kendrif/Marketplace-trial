<h1 class="mb-2 text-2xl text-black font-bold text-center"> </h1>
<h1></h1>
<% if notice %>
  <div class="alert alert-success" role="alert"><%= notice %></div>
<% end %>

<h1>Catalog</h1>

<div class="flex flex-wrap items-start justify-start">
  <% cache @products do %>
    <% @products.where(user_id: @account.id).each do |product| %>
      <% cache product do %>
        <div class=" bg-white relative w-full p-6 mb-6 border-2 border-white shadow sm:shadow-md md:shadow-lg rounded-lg lg:w-1/4 lg:mr-8">
            <h3 class="mb-2 text-2xl text-black font-bold"><%= product.title %></h3>
            <p>
              <%= sanitize(product.description) %>
            </p>
            <div class="price">
            £ <%= (product.price) %>
            </div>
            <div>
            <%= button_to 'Add', line_items_path(product_id: product)%>
            </div>
        </div>
      <% end %>
    <% end %>
  <% end %>
</div>

<div id="cart" class="carts">
